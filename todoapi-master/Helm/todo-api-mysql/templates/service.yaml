apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-api-mysql.fullname" . }}
  labels:
    {{- include "todo-api-mysql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-api-mysql.selectorLabels" . | nindent 4 }}
